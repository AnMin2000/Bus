#pragma once
#include"header.h"
int main()
{
	//Mouse();
	
	Ticket_read();
	Member_read();
	LoginUi();
	//ExClick();
}

	//printf("%s", ticket[ticket_count - 2].month);
	//할일 : 달, 일, 출발지역, 도착 지역 비교하기 전에 reserve함수 반복문 다시 하기
